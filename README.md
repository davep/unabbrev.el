# unabbrev.el

`unabbrev.el` is a small Emacs package that provides the command
`unabbrev-picker`, this is used to provide a simple menu of abbrevs
available in the current buffer (prompted for via `completing-read`). When
selected an abbrev is inserted and then expanded.

[//]: # (READNE.md ends here)
